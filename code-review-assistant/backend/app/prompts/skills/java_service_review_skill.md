## Java 服务审查技能

重点关注：
1. Controller / Service / Mapper 调用链是否存在空指针、越权、参数校验缺失。
2. DTO 与实体映射是否有字段遗漏、默认值歧义、时区/精度问题。
3. 事务与缓存注解组合是否可能导致脏读、双写不一致、缓存击穿。
4. Feign / HTTP 客户端接口签名与对端契约是否一致。
5. 批量/并发路径是否存在性能退化（N+1、全表扫、高频序列化）。
