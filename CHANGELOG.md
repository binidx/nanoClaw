# 变更日志策略

当前仓库不维护按版本展开的完整历史变更日志，这个文件用于说明记录策略。

## 当前约定

- 以 Git 提交历史和合并记录作为事实来源
- 影响使用方式、部署方式或安全边界的改动，必须同步更新 `README.md` 或 `docs/`
- 发布说明、迁移说明或重大行为变更，优先写进对应专题文档，而不是只留在提交信息里

## 何时补充显式日志

出现以下情况时，建议在后续版本中补充正式 release notes：

- 破坏兼容性的配置变更
- 运行脚本、启动方式或数据目录变化
- 新增高风险能力，例如 hooks、终端、远端同步、扩展安装
- 删除旧模块或重命名主要页面

在没有正式版本编排前，请把“面向使用者的重要变化”直接写进相关中文文档。
