apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nanoclaw-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # Set this to your cluster StorageClass when needed.
  # storageClassName: standard
